ServerRoot "/usr/local/apache2"
Listen 80

# Load essential core modules
LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authn_core_module modules/mod_authn_core.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule log_config_module modules/mod_log_config.so

# Load REQUIRED modules for proxying
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so
LoadModule rewrite_module modules/mod_rewrite.so

TypesConfig conf/mime.types

# Define document root for your Vue files
DocumentRoot "/usr/local/apache2/htdocs"
<Directory "/usr/local/apache2/htdocs">
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
    RewriteEngine On
    RewriteBase /
    RewriteRule ^index\.html$ - [L]
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.html [L]
</Directory>

# --- PROXY CONFIGURATION ---
# Turn off forward proxying for security
ProxyRequests on
ProxyPreserveHost On

# Forward all /api requests to your backend service
# Replace 'http://backend-api-service:5000/' with your actual API URL
ProxyPass /sprintboardapi http://backend:3200/sprintboardapi
