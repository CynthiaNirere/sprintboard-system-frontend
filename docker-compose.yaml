version: '3.8'

networks:
  jira_backend_network:
    external: true # Tells Docker the network already exists

services:
  frontend:
    build: .
    ports:
      - "8080:80"
    networks:
      - jira_backend_network